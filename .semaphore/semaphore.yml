version: v1.0
name: Gradle no docker pipline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: "Build"
    task:
      jobs:
        - name: Gradle Build
          commands:
            - checkout
            - cache restore
            - gradle build
            - gradle test
